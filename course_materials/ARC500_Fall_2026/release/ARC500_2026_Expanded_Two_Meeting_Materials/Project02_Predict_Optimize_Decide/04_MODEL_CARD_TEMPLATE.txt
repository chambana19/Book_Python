ARC 500 — ONE-PAGE MODEL CARD

MODEL NAME / DATE / STUDENT

INTENDED QUESTION AND USE
What does the model estimate or classify? What decision can it inform?

DATA AND ROW MEANING
Exact file/version or hash, provenance, row meaning, sample size, class counts,
feature list with units, target/label rule, and known synthetic-data limits.

SELECTION, SPLITS, AND BASELINE
Fit/validation/final-test sizes and indexes; random_state/group/time logic;
what was selected on validation or out-of-fold evidence; baseline strategy.
Confirm that final-test labels were opened only after model/threshold choices froze.

MODEL AND SETTINGS
Estimator, preprocessing, important settings, software version.

EVALUATION
Regression: untouched-final-test MAE, RMSE, R², largest residual, uncertainty method.
Classification: untouched-final-test confusion matrix, precision, recall, F1,
actual/predicted support counts, frozen threshold, threshold-selection set and
consequence rule, probability-calibration status, and Brier score if probabilities
are interpreted.
Include one manual metric check.

SUPPORTED DOMAIN
Training feature ranges, joint combinations represented by the data, joint-support
test and cutoff, and exact feature order expected by the saved artifact.

ARTIFACT AND REPRODUCIBILITY
Artifact filename/hash, estimator/library version, fitted-row count, coefficient or
model settings, feature order, threshold, and files needed to reproduce prediction.

LIMITATIONS AND KNOWN FAILURE MODES
At least three.

REFUSAL / ESCALATION RULE
When should the script refuse prediction or require simulation/expert review?
Include non-finite/wrong-shape input, marginal extrapolation, weak joint support,
uncalibrated probability use, and all optimized candidates pending confirmation.

CLAIMS
Permitted claim:
Claim this card does NOT permit:

AI USE AND VERIFICATION
What AI helped create and how outputs were checked.
